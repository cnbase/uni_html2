/**
 * vue.config.js 配置项
 */
const vueConfig = {}

/**
 * vue-cli 原始babel配置文件
 */
const babelConfig = {
    presets: [
        '@vue/cli-plugin-babel/preset'
    ]
}

module.exports = {
    vueConfig,
    babelConfig
}